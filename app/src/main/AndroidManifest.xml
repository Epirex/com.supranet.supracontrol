<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SupraControl"
        tools:targetApi="31">
        <activity
            android:name=".ProductsListActivity"
            android:exported="false"
            android:label="Lista de Productos"/>
        <activity
            android:name=".ProductsAddActivity"
            android:exported="false"
            android:label="Añadir Productos"/>
        <activity
            android:name=".ScreensActivity"
            android:exported="false"
            android:label="@string/title_activity_screens2" />
        <activity
            android:name=".MenuActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>